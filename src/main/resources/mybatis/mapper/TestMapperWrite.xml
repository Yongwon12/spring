<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.model.aws.dao.TestMapper">

    <select id="getAllDataList" resultType="com.example.demo.model.aws.dto.Portfolio">
        INSERT INTO boardwrite(id, uesrid, title, description, img, writedate, profileimage, nickname, likedcount,
                               commentcount)
        VALUES (#{id}, #{uesrid}, #{title}, #{description}, #{img}, #{writedate}, #{profileimage}, #{nickname},
                #{likedcount}, #{commentcount});
    </select>

</mapper>